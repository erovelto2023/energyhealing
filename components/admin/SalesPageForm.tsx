export default function SalesPageForm({ initialData, onComplete }: { initialData?: any; onComplete?: () => void }) {
    return (
        <div className="p-8 bg-yellow-50 border border-yellow-200 rounded-lg">
            <p className="text-yellow-800">Sales page management feature is not available in this version.</p>
        </div>
    );
}
